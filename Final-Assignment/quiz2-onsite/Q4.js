// 4. Swap - Do a swap on the sentence using the arguments provided and 
// return the new sentence.
// - `First argument` is the sentence to perform the `search` and `replace` on (sentence)
// - `Second argument` is the word that you want to `change` (before)
// - `Third argument` is what you will be `changing` the second argument with (after)
// - `Preserve the case of the first character` in the original word when you are 
// replacing it. For example if you mean to replace the word "Book" with the word "dog", it should be replaced as "Dog"

function swap(arg1, arg2, arg3) {
    case 1:
        ar1 = ar1.replace(arg2)
        break;
    case 2:
        // code block
        break;
    case 3:
        // code block
        break;
    default:
        // code block

}

swap()